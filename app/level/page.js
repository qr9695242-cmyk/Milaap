"use client"; import {useAuth} from '@/lib/AuthContext'; import Link from 'next/link'; import BottomNav from '@/components/BottomNav';
export default function Level(){const {profile}=useAuth(); const level=Math.max(1,Math.floor((profile?.totalCoinsGifted||0)/40000)+1); const progress=((profile?.totalCoinsGifted||0)%40000)/40000*100; return <main className="min-h-screen bg-void pb-28"><header className="px-5 pt-7"><Link href="/profile" className="text-ink/70">‹</Link><h1 className="mt-2 font-display text-2xl font-black text-ink">🔥 Level</h1></header><section className="mx-5 mt-5 rounded-2xl bg-glow-gradient p-6"><p className="text-xs text-ink/80">Current Level</p><p className="mt-1 text-4xl font-black text-ink">Lv.{level}</p><div className="mt-5 h-2 rounded-full bg-black/20"><div className="h-full rounded-full bg-white" style={{width:`${progress}%`}}/></div><p className="mt-2 text-[11px] text-ink/80">Gift level scale is now 8× higher.</p></section><BottomNav/></main>}
