"use client";
import Link from "next/link"; import {TABLES} from "@/lib/premiumCatalog"; import {useState} from "react"; import BottomNav from "@/components/BottomNav";
export default function LiveMatch(){const [joined,setJoined]=useState(null);return <main className="min-h-screen bg-void pb-28"><header className="px-5 pt-7"><Link href="/profile" className="text-ink/70">‹</Link><h1 className="mt-2 font-display text-2xl font-black text-ink">📺 Live Match</h1><p className="text-xs text-mist">6 tables • 6 seats each</p></header><div className="mx-5 mt-5 grid grid-cols-2 gap-3">{TABLES.map(t=><button key={t.id} onClick={()=>setJoined(t.id)} className="premium-card p-4 text-left"><p className="font-bold text-ink">{t.name}</p><p className="mt-1 text-xs text-emerald-400">● Open</p><p className="mt-2 text-[10px] text-mist">0/6 players</p><span className="mt-3 block rounded-full bg-glow-gradient py-2 text-center text-[10px] font-bold text-ink">Join</span></button>)}</div>{joined&&<div className="mx-5 mt-5 premium-card p-4"><p className="font-bold text-ink">Table joined</p><p className="mt-1 text-xs text-mist">Waiting for players at {joined.replace('table-','Table ')}.</p></div>}<BottomNav/></main>}
