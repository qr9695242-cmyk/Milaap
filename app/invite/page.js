"use client"; import {useState} from "react"; import Link from "next/link"; import BottomNav from "@/components/BottomNav";
export default function Invite(){const [copied,setCopied]=useState(false); const code='MILAAP2026'; async function copy(){try{await navigator.clipboard.writeText(code);setCopied(true)}catch{}} return <main className="min-h-screen bg-void pb-28"><header className="px-5 pt-7"><Link href="/profile" className="text-ink/70">‹</Link><h1 className="mt-2 font-display text-2xl font-black text-ink">👥 Invite Friends</h1></header><section className="mx-5 mt-5 rounded-2xl bg-glow-gradient p-6 text-center"><p className="text-5xl">🎁</p><p className="mt-3 font-display text-xl font-black text-ink">Earn Coins</p><p className="mt-1 text-xs text-ink/80">Share your invite code with friends.</p><div className="mt-5 rounded-xl bg-white/30 p-3 font-black tracking-[.3em] text-ink">{code}</div><button onClick={copy} className="mt-4 w-full rounded-full bg-white py-3 text-sm font-bold text-void">{copied?'Copied!':'Copy Code'}</button></section><BottomNav/></main>}
